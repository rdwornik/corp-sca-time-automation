# Python Environment — corp-sca-time-automation

- Python >=3.12
- Virtual env: .venv\Scripts\Activate.ps1
- Install: pip install -r requirements.txt
- NOTE: This repo uses requirements.txt (pyproject.toml migration pending)
- Functional style preferred (no classes unless necessary)
- TypedDict for type hints
- argparse CLI via run.py
