# Azure IDs (optional, for SharePoint upload)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
SHAREPOINT_SITE_ID=
SHAREPOINT_LIST_ID=

# Graph API token — leave blank to auto-acquire via 'az login' session.
# Set manually only if az CLI is unavailable: GRAPH_ACCESS_TOKEN=eyJ...
GRAPH_ACCESS_TOKEN=
