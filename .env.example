# Azure IDs (optional, for SharePoint upload)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
SHAREPOINT_SITE_ID=
SHAREPOINT_LIST_ID=

# Graph API token is auto-acquired via 'az login' session (azure-identity).
# No manual token needed. Just run: az login
